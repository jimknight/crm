The import prospects failed from this URL:
Note: You may not have access to the data in this URL from your IP
  <%= @import_url %>

You can test the JSON content in a validator:
  http://jsonlint.com/

The JSON import that failed is shown below:
  <%= @invalid_json_text %>