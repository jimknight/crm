<h2>Active Clients</h2>
<p><%= link_to "View archived clients", archived_clients_path %></p>
<p><%= link_to 'New Client', new_client_path, :class => "btn btn-primary" %></p>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>State</th>
      <th>City</th>
      <th>Zip</th>
      <th>Phone</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= form_tag clients_path, :method => 'get' do %>
        <%= search_field_tag :search_name, params[:search_name], :placeholder => "search" %>
        <% end %></td>
      <td><%= form_tag clients_path, :method => 'get' do %><%= search_field_tag :search_state, params[:search_state], :placeholder => "search" %><% end %></td>
      <td><%= form_tag clients_path, :method => 'get' do %><%= search_field_tag :search_city, params[:search_city], :placeholder => "search" %><% end %></td>
      <td><%= form_tag clients_path, :method => 'get' do %><%= search_field_tag :search_zip, params[:search_zip], :placeholder => "search" %><% end %></td>
      <td><%= form_tag clients_path, :method => 'get' do %><%= search_field_tag :search_phone, params[:search_phone], :placeholder => "search" %><% end %></td>
    </tr>
    <% @clients.each do |client| %>
      <tr>
        <td><%= link_to client.name, client %></td>
        <td><%= client.state %></td>
        <td><%= client.city %></td>
        <td><%= client.zip %></td>
        <td><%= client.phone %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div style="background:#F5F5F5;padding:0 10px;border-radius:5px;">
  <%= paginate @clients %>
</div>
