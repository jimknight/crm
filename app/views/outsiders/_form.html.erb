<%= simple_form_for(@outsider) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <% if Outsider.count == 0 %>
      <%= f.input :email, :as => :email, :placeholder => "externaluser@company.com" %>
    <% else %>
      <%= f.input :email, :as => :email, :input_html => { :class => "form-control" }, :url => autocomplete_outsider_email_prospects_path, :as => :autocomplete, :placeholder => "externaluser@company.com" %>
    <% end %>
    <%= f.input :first_name, :as => :text, :placeholder => "optional" %>
    <%= f.input :last_name, :as => :text, :placeholder => "optional" %>
    <%= f.input :client, :as => :hidden, :input_html => { :value => @prospect.id } %>
  </div>

  <div class="form-group">
    <%= button_tag(type: 'submit', :class => "btn btn-primary") do %>
      <i class="fas fa-check"></i> Save
    <% end %>
    <%= link_to :back, :class => "btn btn-default" do %>
      <i class="fas fa-times"></i> Cancel
    <% end %>
  </div>

<% end %>