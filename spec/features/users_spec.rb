require 'rails_helper'

describe "User" do
  it "should attach the activity to the current user", :js => true do
    # User.destroy_all
    # @client = Client.create!(:name => "SGA", :city => "Hillsborough", :state => "NJ")
    # @contact = Contact.create!(:name => "Wayne Scarano")
    # @client.contacts << @contact
    # @user = User.create!(:email => "wscarano@sga.com", :password => "ilovesga", :password_confirmation => "ilovesga")
    # @user.activities.count.should == 0
    # visit new_activity_path
    # fill_in "Email", :with => "wscarano@sga.com"
    # fill_in "Password", :with => "ilovesga"
    # click_button "Sign in"
    # select "SGA", :from => "Client"
    # select "Wayne Scarano", :from => "Contact"
    # fill_in "Comments", :with => "i am wayne"
    # click_button "Save"
    # visit
  end
end